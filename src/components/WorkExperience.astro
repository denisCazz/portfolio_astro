---
// src/components/WorkExperience.astro
const experiences = [
  {
    id: "tesisquare",
    title: "Supply Chain IT Developer",
    company: "Tesisquare",
    location: "Italia",
    period: "2021 - Presente",
    type: "Full-time",
    description: "Sviluppatore software specializzato in soluzioni per la gestione della supply chain, con particolare attenzione alla logistica e al trasporto merci. Responsabile dello sviluppo di applicazioni enterprise per l'ottimizzazione dei processi logistici.",
    responsibilities: [
      "Sviluppo di applicazioni web per la gestione della supply chain",
      "Integrazione di sistemi ERP e WMS",
      "Ottimizzazione dei processi logistici attraverso soluzioni IT",
      "Collaborazione con team internazionali"
    ],
    technologies: ["JavaScript", "Python", "SQL", "Oracle", "Cloud Services"],
    icon: "fas fa-truck",
    color: "var(--secondary)"
  },
  {
    id: "freelance",
    title: "Freelance Web Developer",
    company: "Progetti Personali",
    location: "Remote",
    period: "2019 - Presente",
    type: "Part-time",
    description: "Realizzazione di siti web personalizzati per piccole e medie imprese, con focus su design moderno, performance e user experience. Specializzato in soluzioni full-stack innovative.",
    responsibilities: [
      "Progettazione e sviluppo di siti web responsive",
      "Consulenza tecnica e strategica per clienti",
      "Implementazione di soluzioni e-commerce",
      "Ottimizzazione SEO e performance"
    ],
    technologies: ["React", "Vue.js", "Node.js", "Astro", "TypeScript"],
    icon: "fas fa-laptop-code",
    color: "var(--accent)"
  }
];

const clientProjects = [
  {
    name: "LangAmore Pollenzo",
    description: "Sito web elegante per attività gastronomica locale con focus su tradizione e qualità",
    url: "https://www.langamorepollenzo.it/",
    image: "🍷",
    technologies: ["WordPress", "PHP", "Custom CSS"]
  },
  {
    name: "Sartoria Kristina",
    description: "Portale moderno per sartoria con design personalizzato e gallery interattiva",
    url: "https://sartoriakristina.it/",
    image: "✂️",
    technologies: ["HTML5", "CSS3", "JavaScript"]
  }
];
---

<section id="experience" class="experience-section">
  <div class="container">
    <div class="section-header text-center">
      <h2>Esperienza Lavorativa</h2>
      <p class="section-description">
        Il mio percorso professionale nel mondo dello sviluppo software e delle tecnologie innovative
      </p>
    </div>

    <div class="timeline">
      {experiences.map((exp, index) => (
        <div class="timeline-item" data-aos="fade-up" data-aos-delay={index * 200}>
          <div class="timeline-marker">
            <div class="timeline-icon" style={`background: ${exp.color}`}>
              <i class={exp.icon}></i>
            </div>
          </div>
          
          <div class="timeline-content glass glass-hover">
            <div class="experience-header">
              <div class="experience-title">
                <h3>{exp.title}</h3>
                <div class="company-info">
                  <span class="company">{exp.company}</span>
                  <span class="location">
                    <i class="fas fa-map-marker-alt"></i>
                    {exp.location}
                  </span>
                </div>
              </div>
              <div class="experience-meta">
                <span class="period">{exp.period}</span>
                <span class="type" style={`color: ${exp.color}`}>{exp.type}</span>
              </div>
            </div>
            
            <p class="experience-description">{exp.description}</p>
            
            <div class="responsibilities">
              <h4>Responsabilità principali:</h4>
              <ul>
                {exp.responsibilities.map((resp) => (
                  <li>{resp}</li>
                ))}
              </ul>
            </div>
            
            <div class="technologies">
              <h4>Tecnologie utilizzate:</h4>
              <div class="tech-tags">
                {exp.technologies.map((tech) => (
                  <span class="tech-tag">{tech}</span>
                ))}
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<section class="client-work-section">
  <div class="container">
    <div class="section-header text-center">
      <h2>Progetti Cliente</h2>
      <p class="section-description">
        Siti web personalizzati realizzati per clienti, con focus su design moderno e funzionalità ottimizzate
      </p>
    </div>

    <div class="projects-grid">
      {clientProjects.map((project, index) => (
        <div class="project-card glass glass-hover" data-aos="fade-up" data-aos-delay={index * 200}>
          <div class="project-icon">
            <div class="project-emoji">{project.image}</div>
          </div>
          
          <h3 class="project-name">{project.name}</h3>
          <p class="project-description">{project.description}</p>
          
          <div class="project-tech">
            {project.technologies.map((tech) => (
              <span class="tech-badge">{tech}</span>
            ))}
          </div>
          
          <a href={project.url} target="_blank" rel="noopener noreferrer" class="btn btn-secondary project-link">
            <i class="fas fa-external-link-alt"></i>
            Visita il sito
          </a>
        </div>
      ))}
    </div>

    <div class="cta-section text-center">
      <div class="cta-content glass">
        <h3>Interessato a lavorare insieme?</h3>
        <p>Creiamo insieme il tuo sito web personalizzato, ottimizzato per ogni esigenza</p>
        <a href="#contact" class="btn btn-primary">
          <i class="fas fa-envelope"></i>
          Contattami
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .experience-section {
    background: var(--bg-primary);
    position: relative;
  }

  .client-work-section {
    background: var(--bg-secondary);
  }

  .section-header {
    margin-bottom: var(--space-16);
  }

  .section-description {
    max-width: 600px;
    margin: 0 auto;
    font-size: 1.125rem;
    color: var(--text-secondary);
  }

  /* Timeline Styles */
  .timeline {
    position: relative;
    max-width: 1000px;
    margin: 0 auto;
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(180deg, var(--primary), var(--secondary), var(--accent));
    transform: translateX(-50%);
  }

  .timeline-item {
    position: relative;
    margin-bottom: var(--space-12);
  }

  .timeline-item:nth-child(odd) .timeline-content {
    margin-right: 60%;
    text-align: right;
  }

  .timeline-item:nth-child(even) .timeline-content {
    margin-left: 60%;
    text-align: left;
  }

  .timeline-marker {
    position: absolute;
    left: 50%;
    top: 2rem;
    transform: translateX(-50%);
    z-index: 2;
  }

  .timeline-icon {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
    box-shadow: 0 0 20px rgba(0, 217, 255, 0.3);
    border: 3px solid var(--bg-primary);
  }

  .timeline-content {
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    position: relative;
  }

  .timeline-item:nth-child(odd) .timeline-content::after {
    content: '';
    position: absolute;
    right: -15px;
    top: 2rem;
    width: 0;
    height: 0;
    border: 15px solid transparent;
    border-left-color: rgba(255, 255, 255, 0.1);
  }

  .timeline-item:nth-child(even) .timeline-content::after {
    content: '';
    position: absolute;
    left: -15px;
    top: 2rem;
    width: 0;
    height: 0;
    border: 15px solid transparent;
    border-right-color: rgba(255, 255, 255, 0.1);
  }

  .experience-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: var(--space-4);
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .experience-title h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 var(--space-2) 0;
  }

  .company-info {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .company {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--primary);
  }

  .location {
    font-size: 0.875rem;
    color: var(--text-secondary);
    display: flex;
    align-items: center;
    gap: var(--space-1);
  }

  .experience-meta {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: var(--space-1);
  }

  .period {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .type {
    font-size: 0.875rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .experience-description {
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: var(--space-6);
  }

  .responsibilities,
  .technologies {
    margin-bottom: var(--space-6);
  }

  .responsibilities h4,
  .technologies h4 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-3);
  }

  .responsibilities ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .responsibilities li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-2);
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .responsibilities li::before {
    content: '▶';
    position: absolute;
    left: 0;
    color: var(--primary);
    font-size: 0.75rem;
  }

  .tech-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .tech-tag {
    padding: var(--space-2) var(--space-3);
    background: var(--bg-tertiary);
    color: var(--text-primary);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    font-weight: 500;
    border: 1px solid var(--border-primary);
  }

  /* Client Projects */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--space-8);
    margin-bottom: var(--space-12);
  }

  .project-card {
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    text-align: center;
  }

  .project-icon {
    margin-bottom: var(--space-6);
  }

  .project-emoji {
    font-size: 4rem;
    display: inline-block;
    padding: var(--space-4);
    background: var(--bg-tertiary);
    border-radius: 50%;
    border: 2px solid var(--border-primary);
  }

  .project-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-3);
  }

  .project-description {
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: var(--space-6);
  }

  .project-tech {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
  }

  .tech-badge {
    padding: var(--space-1) var(--space-3);
    background: var(--bg-tertiary);
    color: var(--text-secondary);
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 500;
    border: 1px solid var(--border-secondary);
  }

  .project-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
  }

  /* CTA Section */
  .cta-section {
    margin-top: var(--space-16);
  }

  .cta-content {
    max-width: 500px;
    margin: 0 auto;
    padding: var(--space-12) var(--space-8);
    border-radius: var(--radius-xl);
  }

  .cta-content h3 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-4);
  }

  .cta-content p {
    color: var(--text-secondary);
    margin-bottom: var(--space-8);
    font-size: 1.125rem;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .timeline::before {
      left: 30px;
    }

    .timeline-item:nth-child(odd) .timeline-content,
    .timeline-item:nth-child(even) .timeline-content {
      margin-left: 80px;
      margin-right: 0;
      text-align: left;
    }

    .timeline-marker {
      left: 30px;
    }

    .timeline-icon {
      width: 50px;
      height: 50px;
      font-size: 1.25rem;
    }

    .timeline-item:nth-child(odd) .timeline-content::after,
    .timeline-item:nth-child(even) .timeline-content::after {
      left: -15px;
      border-right-color: rgba(255, 255, 255, 0.1);
      border-left-color: transparent;
    }

    .experience-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .experience-meta {
      align-items: flex-start;
    }

    .projects-grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .project-card {
      padding: var(--space-6);
    }

    .cta-content {
      padding: var(--space-8) var(--space-6);
    }

    .cta-content h3 {
      font-size: 1.5rem;
    }
  }
</style>