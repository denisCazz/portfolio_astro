---
// src/components/WorkExperience.astro
const jobs = [
  {
    title: "Web Developer",
    company: "Azienda X",
    years: "2022 - Presente",
    description: "Sviluppo di applicazioni web innovative con tecnologie all'avanguardia. Collaborazione con team di designer e sviluppatori per creare esperienze utente di alta qualità. Implementazione di soluzioni di backend robuste e scalabili.",
  },
  {
    title: "Frontend Developer",
    company: "Startup Y",
    years: "2021 - 2022",
    description: "Creazione di interfacce utente accattivanti e responsive per diverse piattaforme. Ottimizzazione delle prestazioni e dell'accessibilità dei siti web. Collaborazione con il team di marketing per migliorare la user experience.",
  },
  // ... altri lavori
];
---

<section id="work" class="work">
  <h2><span class="icon">💼</span> Esperienze Lavorative</h2>
  <div class="work-grid">
    {jobs.map((job) => (
      <div class="work-card">
        <div class="work-header">
          <h3>{job.title}</h3>
          <span class="company">{job.company}</span>
          <span class="years">{job.years}</span>
        </div>
        <p class="description">{job.description}</p>
      </div>
    ))}
  </div>
</section>

<style>
  /* ... variabili di colore (se non definite globalmente) ... */
  :root {
    --primary: #00ffea; 
    --secondary: #ff00f7; 
    --background: #0d0d0d; 
    --accent: #ffffff; 
    --border-color: rgba(255, 255, 255, 0.1);
  }

  .work {
    padding: 4rem 2rem;
    background-color: var(--background);
  }

  .work h2 {
    text-align: center;
    font-size: 2.5rem;
    color: var(--accent);
    margin-bottom: 3rem;
  }

  .work-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .work-card {
    background-color: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border-color);
    padding: 2rem;
    border-radius: 10px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .work-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 0 15px rgba(0, 255, 234, 0.3);
  }

  .work-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .work-header h3 {
    font-size: 1.3rem;
    color: var(--accent);
    margin: 0;
  }

  .company {
    font-size: 1rem;
    color: var(--primary);
  }

  .years {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .description {
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.5;
  }
</style>