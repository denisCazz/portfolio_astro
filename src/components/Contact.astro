---
// src/components/Contact.astro
---

<section id="contact" class="contact-section">
  <div class="container">
    <div class="section-header text-center">
      <h2>Mettiamoci in Contatto</h2>
      <p class="section-description">
        Hai un progetto in mente? Discutiamo insieme di come posso aiutarti a realizzarlo
      </p>
    </div>

    <div class="contact-content">
      <div class="contact-card glass glass-hover" data-aos="fade-up">
        <div class="contact-icon">
          <i class="fas fa-envelope"></i>
        </div>
        
        <h3>Email</h3>
        <p>Scrivimi per discutere del tuo progetto</p>
        <a href="mailto:deniscazzulo@icloud.com" class="contact-link">
          deniscazzulo@icloud.com
        </a>
      </div>

      <div class="social-section" data-aos="fade-up" data-aos-delay="200">
        <h3>Seguimi sui Social</h3>
        <div class="social-links">
          <a href="https://www.linkedin.com/in/denis-cazzulo-b56b551a9/" target="_blank" rel="noopener noreferrer" class="social-link">
            <i class="fab fa-linkedin-in"></i>
            <span>LinkedIn</span>
          </a>
          <a href="https://github.com/deniscazz" target="_blank" rel="noopener noreferrer" class="social-link">
            <i class="fab fa-github"></i>
            <span>GitHub</span>
          </a>
        </div>
      </div>
    </div>

    <div class="cta-section text-center" data-aos="fade-up" data-aos-delay="400">
      <div class="cta-content glass">
        <h3>Pronto a iniziare?</h3>
        <p>Trasformiamo le tue idee in realtà digitale</p>
        <a href="mailto:deniscazzulo@icloud.com" class="btn btn-primary btn-lg">
          <i class="fas fa-rocket"></i>
          Iniziamo il Progetto
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-section {
    background: var(--bg-primary);
    position: relative;
    overflow: hidden;
  }

  .section-header {
    margin-bottom: var(--space-16);
  }

  .section-description {
    max-width: 600px;
    margin: 0 auto;
    font-size: 1.125rem;
    color: var(--text-secondary);
  }

  .contact-content {
    max-width: 800px;
    margin: 0 auto var(--space-16);
  }

  .contact-card {
    padding: var(--space-12);
    border-radius: var(--radius-xl);
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .contact-icon {
    margin-bottom: var(--space-6);
  }

  .contact-icon i {
    font-size: 3rem;
    color: var(--primary);
    padding: var(--space-6);
    background: var(--bg-tertiary);
    border-radius: 50%;
    border: 2px solid var(--border-primary);
  }

  .contact-card h3 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-3);
  }

  .contact-card p {
    color: var(--text-secondary);
    margin-bottom: var(--space-6);
    font-size: 1.125rem;
  }

  .contact-link {
    display: inline-block;
    color: var(--primary);
    text-decoration: none;
    font-size: 1.25rem;
    font-weight: 600;
    padding: var(--space-3) var(--space-6);
    border: 2px solid var(--primary);
    border-radius: var(--radius-lg);
    transition: all var(--transition-normal);
    background: transparent;
  }

  .contact-link:hover {
    background: var(--primary);
    color: var(--bg-primary);
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(0, 217, 255, 0.3);
  }

  .social-section {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .social-section h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-8);
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: var(--space-6);
  }

  .social-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-6);
    border-radius: var(--radius-lg);
    background: var(--bg-tertiary);
    border: 1px solid var(--border-primary);
    color: var(--text-primary);
    text-decoration: none;
    transition: all var(--transition-normal);
    min-width: 120px;
  }

  .social-link:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0, 217, 255, 0.2);
    border-color: var(--primary);
  }

  .social-link i {
    font-size: 2rem;
    color: var(--primary);
  }

  .social-link span {
    font-size: 0.875rem;
    font-weight: 500;
  }

  .cta-section {
    margin-top: var(--space-16);
  }

  .cta-content {
    max-width: 500px;
    margin: 0 auto;
    padding: var(--space-12) var(--space-8);
    border-radius: var(--radius-xl);
  }

  .cta-content h3 {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-4);
  }

  .cta-content p {
    color: var(--text-secondary);
    margin-bottom: var(--space-8);
    font-size: 1.125rem;
  }

  .btn-lg {
    font-size: 1.125rem;
    padding: var(--space-4) var(--space-8);
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .contact-card {
      padding: var(--space-8) var(--space-6);
    }

    .social-links {
      flex-direction: column;
      align-items: center;
      gap: var(--space-4);
    }

    .social-link {
      min-width: 200px;
    }

    .cta-content {
      padding: var(--space-8) var(--space-6);
    }

    .cta-content h3 {
      font-size: 1.75rem;
    }

    .btn-lg {
      font-size: 1rem;
      padding: var(--space-3) var(--space-6);
    }
  }
</style>